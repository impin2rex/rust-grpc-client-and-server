syntax = "proto3";

package local;

service TimeProducer {
  rpc StreamTimes(stream Empty) returns (stream TimeMessage);
}

message Empty {}

message TimeMessage {
  string seconds = 1;
  int32 nanos = 2;
}
